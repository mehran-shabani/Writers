# =============================================================================
# Docker Compose Environment Variables
# =============================================================================
# این فایل شامل متغیرهای محیطی مخصوص استقرار با Docker Compose است
# 
# استفاده:
#   1. این فایل را به .env کپی کنید:
#      cp .env.docker .env
#   2. مقادیر را مطابق با محیط خود تنظیم کنید
#   3. Docker Compose به‌صورت خودکار فایل .env را می‌خواند
# =============================================================================

# -----------------------------------------------------------------------------
# PostgreSQL Configuration
# -----------------------------------------------------------------------------
POSTGRES_DB=myapp_production
POSTGRES_USER=myapp_user
POSTGRES_PASSWORD=CHANGE_THIS_SECURE_PASSWORD_FOR_POSTGRES
POSTGRES_MAX_CONNECTIONS=100

# -----------------------------------------------------------------------------
# Redis Configuration
# -----------------------------------------------------------------------------
REDIS_PASSWORD=CHANGE_THIS_SECURE_PASSWORD_FOR_REDIS
REDIS_CACHE_DB=0
REDIS_SESSION_DB=1
REDIS_QUEUE_DB=2

# -----------------------------------------------------------------------------
# Application Environment
# -----------------------------------------------------------------------------
NODE_ENV=production
APP_ENV=production

# -----------------------------------------------------------------------------
# Ports
# -----------------------------------------------------------------------------
FRONTEND_PORT=3000
BACKEND_PORT=8000

# -----------------------------------------------------------------------------
# URLs
# -----------------------------------------------------------------------------
APP_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:8000
API_URL=http://localhost:8000

# -----------------------------------------------------------------------------
# Storage Configuration
# -----------------------------------------------------------------------------
STORAGE_ROOT=/storage
SSD_MOUNT_POINT=/mnt/ssd

# -----------------------------------------------------------------------------
# Security & Authentication
# -----------------------------------------------------------------------------
JWT_SECRET=CHANGE_THIS_TO_A_VERY_LONG_RANDOM_SECRET_KEY_MINIMUM_64_CHARACTERS
JWT_EXPIRATION=7d
JWT_REFRESH_SECRET=CHANGE_THIS_TO_ANOTHER_VERY_LONG_RANDOM_SECRET_KEY_64_CHARS
JWT_REFRESH_EXPIRATION=30d
SESSION_SECRET=CHANGE_THIS_SESSION_SECRET_MINIMUM_64_CHARACTERS

# -----------------------------------------------------------------------------
# CORS
# -----------------------------------------------------------------------------
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# -----------------------------------------------------------------------------
# Worker Configuration
# -----------------------------------------------------------------------------
WORKER_CONCURRENCY=5
WORKER_MAX_RETRIES=3

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
LOG_LEVEL=info
LOG_FORMAT=json

# -----------------------------------------------------------------------------
# Email Configuration (Optional)
# -----------------------------------------------------------------------------
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=your_smtp_user
SMTP_PASSWORD=your_smtp_password
SMTP_FROM_EMAIL=noreply@myapp.com
SMTP_FROM_NAME=MyApp

# -----------------------------------------------------------------------------
# Monitoring (Optional)
# -----------------------------------------------------------------------------
SENTRY_DSN=
SENTRY_ENVIRONMENT=production
METRICS_ENABLED=false
METRICS_PORT=9090

# =============================================================================
# نکات مهم:
# =============================================================================
# 
# 1. رمزهای امن تولید کنید:
#    openssl rand -base64 48
#    python3 -c "import secrets; print(secrets.token_hex(32))"
#
# 2. این فایل را commit نکنید! در .gitignore قرار دارد
#
# 3. برای production از رمزهای قوی و منحصر به فرد استفاده کنید
#
# 4. متغیرهای POSTGRES_PASSWORD و REDIS_PASSWORD حتماً باید تنظیم شوند
#
# 5. برای استقرار production، CORS_ORIGINS را به دامنه واقعی تغییر دهید
#
# =============================================================================
